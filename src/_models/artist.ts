/**
 * An artist consists of a name as a string, popularity rating from 1-100, and genres represented
 * in an arrays of strings. An artist is populated by href from a "simple" artist.
 */
export class Artist {
  name: string;
  popularity: number;
  genres: string[];
}
