/**
 * An album consists of its name, the year it was released, a popularity rating from 1-100, and the
 * genres it has been categorized as an array of strings. It will be populated by the href from a
 * "simple" album.
 */
export class Album {
  name: string;
  year: number;
  popularity: number;
  genres: string[];
}
